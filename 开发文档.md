# 深入地牢 — 开发文档

## 项目概述

这是一款基于 Phaser 4 开发的文字风格 Roguelike 卡牌冒险游戏原型。核心玩法在 3×3 九宫格内展开，通过拖动玩家卡和道具卡与各种卡牌互动。

游戏唯一权威的设计库位于：C:\Users\jinji\Desktop\文档\MyNote\游戏开发项目\深入地牢 需要核对设计时应进行参考

## 技术栈

- **引擎**：Phaser 4.1.0
- **构建工具**：Vite 6
- **语言**：JavaScript (ES Modules)
- **音频**：Web Audio API（纯代码生成，无外部音频文件）
- **视觉**：纯文字渲染，零图片和几何线条，但凡需要类似于线条绘制的地方都要用恰当的文字进行替代

## 目录结构

```
src/
  main.js              — 游戏入口，Phaser.Game 配置，场景注册
  config.js            — 色卡、常量、九宫格几何计算工具
  data/
    classes.js         — 职业定义与玩家初始状态生成
    monsters.js        — 怪物模板、生成规则、分配算法
    items.js           — 道具卡定义与随机生成
    relics.js          — 遗物定义与品质池
    rooms.js           — 房间类型配置与路线规则
  core/
    gameState.js       — 全局状态管理（玩家属性、格子、存档）
    battle.js          — 伤害结算、战斗流程、道具效果
    dungeon.js         — 房间生成、卡牌分布、路线选择
    cards.js           — 卡牌对象工厂函数
  scenes/
    BootScene.js       — 启动画面，音频初始化
    MenuScene.js       — 主菜单、职业选择、设置
    GameScene.js       — 核心游戏场景（九宫格+战斗+UI）
    RoomSelectScene.js — 路线选择（房间类型选项）
    GameOverScene.js   — 死亡/通关结算
  audio/
    AudioManager.js    — Web Audio API 音效与 BGM 管理
  utils/
    tweens.js          — 常用缓动动画 Promise 封装
```

## 核心架构

### 状态管理

游戏状态使用纯 JavaScript 对象，通过场景间传递：

```js
this.scene.start('SceneName', { state: gameState });
```

状态包含：
- `player`：玩家属性（生命、攻击、防御、金币、技能、遗物、道具）
- `layer`：当前层数（1-3）
- `nodeIndex`：当前层节点（0-8）
- `grid`：9个格子的卡牌堆叠数组 `Card[][]`
- `playerGridIndex`：玩家所在格子（默认7，即格8）

### 卡牌对象格式（扁平化模型）

所有卡牌都是扁平化的普通对象，通过 `type` 字段区分，属性直接放在对象顶层：

```js
// 玩家卡 — 仅作为格子中的占位符
{ type: 'player', name: '兵大哥', revealed: true }

// 怪物卡 — 属性在顶层
{ type: 'monster', templateId: 'skeleton', name: '骷髅',
  revealed: false, hp: 6, maxHp: 6, atk: 2, def: 0,
  level: 1, keywords: [], description: '...' }

// 道具卡
{ type: 'item', templateId: 'throwingKnife', name: '飞刀',
  revealed: true, effect: 'damage', value: 6, description: '...' }

// 机关卡
{ type: 'trap', name: '弩箭机关', revealed: false, templateId: 'crossbow',
  hp: 2, maxHp: 2, atk: 0, def: 0, description: '...', triggered: false }

// 其他：goldCard, chest, food, mentor, attrUp, shopItem
```

### 场景阶段状态机

GameScene 使用阶段枚举防止并发操作导致卡死：

```js
const Phase = { IDLE, BUSY, MODAL, TRANSITION, OVER };
```

- **IDLE**：接受拖拽输入
- **BUSY**：正在处理拖放结果（战斗/互动动画）
- **MODAL**：显示选择弹窗（遗物/属性选择）
- **TRANSITION**：房间清理过渡中
- **OVER**：游戏结束

### 九宫格系统

- 格子编号 0-8，对应「格1」到「格9」
- 每个格子是一个卡牌堆叠数组，最上方的卡牌可见/可交互
- 玩家出生在 index 7（格8）
- 正交相邻（上下左右）的格子会随玩家移动自动翻转最上方卡牌
- 每个格子由 Graphics 背景 + 主文本 + 副文本 + 堆叠计数组成，通过 `refreshCell()` 增量更新

### 拖拽规则

| 卡牌类型 | 可拖动 | 拖到空白格 | 拖到卡牌上 |
|---------|--------|-----------|-----------|
| 玩家卡 | ✅ | 移动 | 互动 |
| 道具卡 | ✅ | 场上道具收入道具栏 | 对合法目标使用道具 |
| 其他 | ❌ | — | — |

- 拖拽命中以九宫格格子矩形区域为准
- 道具栏内的道具也可直接拖到九宫格目标上使用
- 阶段非 IDLE 时拖拽自动拒绝

### 战斗结算流程

1. 先攻判定：怪物有 `firstStrike` 词条则怪物先攻，否则玩家先攻
2. 伤害 = 攻击 - 防御，最低为 0
3. 死亡方无法造成伤害
4. 结算后触发：刺皮反击、历战加攻、复仇骷髅buff、硬皮回血等
5. 机关伤害为固定值，不受玩家防御影响

### 房间生成流程

1. 根据房间类型确定额外卡牌（商店商品、宝箱等）
2. 根据节点索引和层数生成怪物（`NODE_MONSTER_RULES`）
3. 随机添加 2-3 张机关卡和 3-5 张道具卡
4. 将所有卡牌洗混后轮转分布到除玩家格外的 8 个格子
5. 每格最大堆叠深度 4 张，保证可用性

### 过渡保护机制

- `transitionScheduled` 标志防止重复触发房间清理/游戏结束
- 追踪者骷髅的异步战斗通过 phase 锁保护
- 传送机关使用安全的重分布算法（轮转放置，限制深度）

## 色卡配置

色卡定义在 `src/config.js` 的 `COLORS` 和 `TYPE_COLORS` 中。

| 用途 | 颜色值 | 说明 |
|------|--------|------|
| 背景 | `#151618` | 全局深灰背景 |
| 玩家 | `#52c6b8` | 青色，玩家卡、正面状态 |
| 怪物 | `#e76457` | 红色，怪物卡、伤害 |
| 道具 | `#74a8ff` | 蓝色，道具卡、交互 |
| 金币/宝箱 | `#f5c86a` | 金色，奖励相关 |
| 食物/属性 | `#7ec86a` | 绿色，治疗、提升 |
| 遗物 | `#9b72cf` | 紫色，特殊效果 |
| 机关 | `#d98e3e` | 橙色，陷阱 |

## 扩展指南

### 添加新怪物

1. 在 `src/data/monsters.js` 的 `MONSTERS` 中添加模板
2. 如需新词条，在 `src/scenes/GameScene.js` 的对应逻辑中添加处理

### 添加新道具

1. 在 `src/data/items.js` 的 `ITEMS` 中添加定义
2. 在 `src/core/battle.js` 的 `useItemOnTarget` 中添加效果处理

### 添加新遗物

1. 在 `src/data/relics.js` 的 `RELICS` 中添加定义
2. 在 `src/core/gameState.js` 的 `getTotalStats` 中属性会自动叠加
3. 如需特殊效果，在对应触发点添加逻辑

### 添加新职业

1. 在 `src/data/classes.js` 的 `CLASSES` 中添加
2. 在 `src/scenes/MenuScene.js` 中会自动显示

## 场景生命周期

```
BootScene → MenuScene → GameScene ↔ RoomSelectScene → GameOverScene
                ↑___________________________________________|
```

## 音频系统

使用单例 `AudioManager`，通过 Web Audio API 实时生成：

- **BGM**：缓慢正弦波琶音（Airship Serenity 风格）
- **音效**：不同波形（sine/triangle/sawtooth/square）和频率组合
- 所有音频无外部文件依赖

## 存档机制

- 使用 `localStorage` 序列化游戏状态
- 存档点：路线选择界面（`RoomSelectScene`）可手动保存
- 自动清除：游戏结束/通关后清除存档

## 移动端适配

- viewport 禁止缩放
- CSS `touch-action: none` 阻止默认触摸行为
- Phaser 的 pointer 事件自动处理触摸拖拽
- 画布使用 `Phaser.Scale.FIT` 自适应屏幕

## 已知限制与 TODO

- 蓝色/金色品质遗物尚未设计内容，目前回退到白色品质
- 勾绳的方向选择交互尚未完整实现（目前拖动到目标即可）
- 翻转卡（swap位置）的交互简化处理
- 照明卡效果简化处理
- 无尽水袋和法则魔杖的主动使用按钮未在UI中单独实现
- 部分怪物词条的复杂交互（如鼓舞的实时buff计算）简化处理
